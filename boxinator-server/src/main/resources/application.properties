# Configuring the Db
spring.datasource.url: jdbc:postgresql://localhost:5432/BoxinatorDB
spring.datasource.username: postgres
spring.datasource.password: Adam2805

# Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.hibernate.ddl-auto=update

#Turn Statistics on
logging.level.org.hibernate.stat=debug

# Enable logging
spring.jpa.show-sql=true

#keycloak docker image
keycloak.auth-server-url=https://keycloak-boxinator2.herokuapp.com/auth/
#keycloak local
#keycloak.auth-server-url=https://localhost:8180/auth
keycloak.realm=boxinator
keycloak.resource=boxinator-client
keycloak.public-client=true
keycloak.principal-attribute=preferred_username
